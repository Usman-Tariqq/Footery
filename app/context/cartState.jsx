import CartContext from "./cartContext";


const CartState = (props) => {

    return (
        <CartState.Provider>
            ({children}) 
        </CartState.Provider>
    )

}

export default CartState; 